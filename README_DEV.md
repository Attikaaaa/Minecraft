# Dev / CI

One-liner to run the full CI flow:

```bash
npm run ci
```

Notes:
- Requires Node.js (18+ recommended) and `npm install` to fetch `playwright`.
- Outputs reports under `reports/run-<timestamp>/`.
